@model List<Contacts.Contact>

@if (Model.Count == 0)
{
    <p>No contacts found.</p>
}
else
{
    <ul>
        @foreach (var c in Model)
        {
            <li>@c.FirstName - @c.LastName</li>
        }
    </ul>
}